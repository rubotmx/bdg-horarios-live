{
  "rules": {
    "//": "Lectura pública (cron remind.js + restock leen vía REST). Escritura general requiere Google Auth.",
    "//-restock": "/restock_snapshot y /restock_pos permiten escritura sin auth porque las APIs serverless las escriben (CRON_SECRET protege el cron de actualización).",

    ".read": true,
    ".write": "auth != null",

    "bdg_horarios_v2": {
      ".read": true,
      ".write": "auth != null",

      "pulse_channels": {
        ".read": true,
        ".write": true
      }
    },

    "restock_snapshot": {
      ".read": true,
      ".write": true
    },

    "restock_pos": {
      ".read": true,
      ".write": true
    }
  }
}
